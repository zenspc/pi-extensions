---
"@zenspc/pi-browser": minor
---

Launch a headed system Chrome against the dedicated User Data Dir on the first tool call. Discover an ephemeral Debug Port from that dir instead of attaching to port 9222. Leave the window running when Pi exits.
