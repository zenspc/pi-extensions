---
"@zenspc/pi-copilot-discovery": patch
---

Load on Pi 0.84.2 with a native `github-copilot` provider wrap (builtin auth + streams, live `/models` catalog, no `availableModelIds` filter). Drop deleted `@earendil-works/pi-ai/oauth` imports.
