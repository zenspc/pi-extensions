---
"@zenspc/pi-quiet": minor
---

Run Compaction and Quiet chrome polish for `@zenspc/pi-quiet`.

Adjacent same-kind success/soft tool rows fold into Group Header + Member Bullets (last-member carrier). Kind Emoji on Quiet Rows and headers. Groups rebuild from session history; whole-group expand to Stock bodies. Visible surfaces keep Stock-matching Tool Shell Background under `renderShell: "self"`.
